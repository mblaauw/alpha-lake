# Pinned image digests for air-gap / offline operation.
# Generated by `just vendor`. Update when cutting a new release.
services:
  postgres:
    image: postgres:17-alpine
  rustfs:
    image: rustfs/rustfs:latest
  otel:
    image: otel/opentelemetry-collector-contrib:latest
